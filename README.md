# CSV_to_EXCEL_Converter
A Python automation project that converts CSV files into Excel format while cleaning and normalizing data. It handles missing values, date parsing, column renaming, and provides a command-line interface with logging and error handling. Built using pandas and openpyxl for efficient data processing.
